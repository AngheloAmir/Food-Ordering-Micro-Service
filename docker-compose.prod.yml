version: '3.8'

services:
  node-backend:
    build:
      context: ./backend/node-service
      target: production
    ports:
      - "8000:8000"
    environment:
      - NODE_ENV=production
    restart: always
    deploy:
      resources:
        limits:
          cpus: '1'
          memory: 512M
