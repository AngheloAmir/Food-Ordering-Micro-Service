services:
  csr-frontend:
    build:
      context: .
      dockerfile: Dockerfile
    image: csr-frontend
    ports:
      - "5200:5200"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - WATCHPACK_POLLING=true
